server.port=8081

# ---- MySQL Database Configuration ----
spring.datasource.url=jdbc:mysql://localhost:3306/quakeguard_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Dhiya@2006

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# File upload limits
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
app.upload.dir=uploads

# JWT Security
app.jwt.secret=quakeguard-ai-production-super-secret-key-2026-secure
app.jwt.expiration-ms=86400000

# CORS Configuration for Vite dev server
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# Google Gemini Multimodal Vision AI Configuration
app.ai.gemini.api-key=${GEMINI_API_KEY:YOUR_GEMINI_API_KEY_HERE}
app.ai.gemini.model=gemini-1.5-flash
app.ai.api-key=${GEMINI_API_KEY:YOUR_GEMINI_API_KEY_HERE}

# Spring Mail (Gmail SMTP configuration for Real OTP Email Delivery)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=disasterguard26@gmail.com
spring.mail.password=hsruqaxyanfijnav
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

